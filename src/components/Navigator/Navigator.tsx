import { styled } from "@fluentui/react/lib/Utilities";

import { NavigatorBase } from "./Navigator.base";
import { NavigatorProps } from "./Navigator.types";

export const Navigator = styled<NavigatorProps, {}, {}>(NavigatorBase, () => ({}), undefined, undefined, true);
